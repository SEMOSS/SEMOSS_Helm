{{- if .Values.security.useStorage}}
kind: PersistentVolume
apiVersion: v1
metadata:
  name: {{ .Values.security.volume.persistentVolumeClaimName }}
  labels:
    app.kubernetes.io/name: {{ printf "%s-%s"  .Values.env .Values.security.name}}
    helm.sh/chart: {{ include "semoss.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  accessModes: 
  - ReadWriteOnce
  capacity:
    storage: {{ .Values.security.storageSize | quote }}
  {{- if .Values.security.storageClass }}
  storageClassName: {{ .Values.security.storageClass | quote }}
  {{- else }}
  storageClassName: ""
  {{- end }}
  persistentVolumeReclaimPolicy: {{ .Values.security.volume.persistentVolumeReclaimPolicy }}
{{- end }}
