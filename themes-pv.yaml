{{- if .Values.themes.useStorage}}
kind: PersistentVolume
apiVersion: v1
metadata:
  name: {{ .Values.themes.volume.persistentVolumeClaimName }}
  labels:
    app.kubernetes.io/name: {{ printf "%s-%s"  .Values.env .Values.themes.name}}
    helm.sh/chart: {{ include "semoss.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  accessModes: 
  - ReadWriteOnce
  capacity:
    storage: {{ .Values.themes.storageSize | quote }}
  {{- if .Values.themes.storageClass }}
  storageClassName: {{ .Values.themes.storageClass | quote }}
  {{- else }}
  storageClassName: ""
  {{- end }}
  persistentVolumeReclaimPolicy: {{ .Values.themes.volume.persistentVolumeReclaimPolicy }}
{{- end }}
